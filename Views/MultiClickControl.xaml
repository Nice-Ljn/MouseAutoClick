<UserControl x:Class="MouseRecorderWpf.Views.MultiClickControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:MouseRecorderWpf.ViewModels"
             xmlns:models="clr-namespace:MouseRecorderWpf.Models"
             xmlns:converters="clr-namespace:MouseRecorderWpf.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:ClickTypeToIndexConverter x:Key="ClickTypeToIndexConverter"/>
        <x:Array x:Key="ClickTypeList" Type="system:String" xmlns:system="clr-namespace:System;assembly=mscorlib">
            <system:String>左键</system:String>
            <system:String>右键</system:String>
            <system:String>双击</system:String>
        </x:Array>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:MultiClickViewModel/>
    </UserControl.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" MinHeight="250"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="当前方案:" Foreground="#909399" FontSize="13" VerticalAlignment="Center"/>
                    <ComboBox x:Name="PlanComboBox" Width="150" Margin="5,0,10,0" 
                              ItemsSource="{Binding PlanNames}" 
                              SelectedItem="{Binding CurrentPlanName, Mode=TwoWay}"
                              SelectionChanged="PlanComboBox_SelectionChanged"/>
                    <Button Content="添加方案" Width="90" Click="BtnAddPlan_Click" Style="{StaticResource PrimaryButton}"/>
                    <Button Content="删除方案" Width="90" Margin="5,0,0,0" Click="BtnDeletePlan_Click" Style="{StaticResource DangerButton}"/>
                    <TextBox x:Name="PlanRenameTextBox" Width="100" Margin="10,0,5,0" Text="重命名" GotFocus="PlanRenameTextBox_GotFocus" LostFocus="PlanRenameTextBox_LostFocus"/>
                    <Button Content="重命名" Width="70" Click="BtnRenamePlan_Click" Style="{StaticResource DefaultButton}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                    <TextBlock Text="当前鼠标位置:" Foreground="#909399" FontSize="13"/>
                    <TextBlock Text="{Binding CurrentPosition}" Foreground="#409EFF" FontWeight="SemiBold" FontSize="13" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="添加目标点 (F4)" Width="150" Click="BtnAdd_Click" Style="{StaticResource PrimaryButton}"/>
                    <TextBlock Text="提示: 移动鼠标到目标位置，按F4或点击按钮添加" Foreground="#909399" VerticalAlignment="Center" Margin="15,0,0,0"/>
                </StackPanel>
            </StackPanel>

            <GroupBox Grid.Row="1" Header="目标点列表">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <DataGrid x:Name="TargetGrid" Grid.Column="0" ItemsSource="{Binding Targets}" 
                            AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                            SelectedItem="{Binding SelectedTarget}" SelectedIndex="{Binding SelectedIndex}"
                            HeadersVisibility="Column" GridLinesVisibility="Horizontal" IsReadOnly="False"
                            BorderThickness="1" BorderBrush="#DCDFE6" Padding="5" MinHeight="200" MaxHeight="300">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="X" Binding="{Binding Position.X}" Width="70" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Y" Binding="{Binding Position.Y}" Width="70" IsReadOnly="True"/>
                            <DataGridComboBoxColumn Header="点击类型" Width="90" 
                                                    SelectedItemBinding="{Binding ClickTypeName}"
                                                    ItemsSource="{Binding Source={StaticResource ClickTypeList}}"/>
                            <DataGridTextColumn Header="延迟(ms)" Binding="{Binding Delay}" Width="75"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Column="1" Margin="15,0,0,0">
                        <Button Content="▲ 上移" Margin="0,0,0,5" Width="90" Click="BtnUp_Click" Style="{StaticResource DefaultButton}"/>
                        <Button Content="▼ 下移" Margin="0,0,0,5" Width="90" Click="BtnDown_Click" Style="{StaticResource DefaultButton}"/>
                        <Button Content="删除选中" Margin="0,0,0,5" Width="90" Click="BtnRemove_Click" Style="{StaticResource WarningButton}"/>
                        <Button Content="清空全部" Width="90" Click="BtnClear_Click" Style="{StaticResource DangerButton}"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="2" Header="执行设置">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="循环次数" FontWeight="SemiBold" Foreground="#606266"/>
                            <TextBlock Text=" (0表示无限循环)" Foreground="#909399" FontSize="12" Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBox Text="{Binding RepeatCount, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,0,0">
                        <TextBlock Text="循环间隙(ms)" Margin="0,0,0,6" FontWeight="SemiBold" Foreground="#606266"/>
                        <TextBox Text="{Binding LoopInterval, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,15,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Content="开始 (F5)" Click="BtnStart_Click" Style="{StaticResource SuccessButton}"/>
                            <Button Content="停止 (F6)" Grid.Column="2" Click="BtnStop_Click" Style="{StaticResource DangerButton}"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>
    </ScrollViewer>
</UserControl>
